import type { Metadata } from "next";

const metaRootInfo = {
    title: "Stripe Payment System",
    description: "Product payment system.",
}

export const metaRootData: Metadata = { ...metaRootInfo };